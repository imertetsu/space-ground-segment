# SpaceGroundSegment

> Ground-segment system for spacecraft operations. **Scope and stack are pending
> the project prompt** — this repo currently holds only the methodology scaffold.

## Status

Commit zero. Stack-agnostic initial setup in place; awaiting the project brief
before any feature code.

## How we work

This project follows the **SSS (Spec → Sketch → Ship)** methodology for
AI-agent-driven development. The full contract lives in [`CLAUDE.md`](CLAUDE.md).
Current project state is in [`docs/HANDOFF.md`](docs/HANDOFF.md).

- Specs are ephemeral, one per in-flight epic, under `docs/specs/`.
- Work ships phase-by-phase; each phase passes the quality gates before it lands.
- Agent roles and their boundaries are defined in `.claude/agents/`.

## Getting started

_TBD — build/run instructions land when the stack is chosen._
