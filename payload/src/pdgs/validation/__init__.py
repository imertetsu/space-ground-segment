"""Validation of the derived L2 against the official L2 SST: co-location,
statistics (bias, RMSE, std, match count, % agreement), and a human-readable
report (REQ-VAL).

The pipeline fails when statistics fall outside configured thresholds
(REQ-VAL-03). Phase 0 skeleton.
"""
