"""Versioned configuration: parameters, coefficients, thresholds (REQ-CFG).

This is the foundation layer — it imports no other PDGS package. Processor and
config versions are stamped onto every product so reruns are reproducible.
"""
